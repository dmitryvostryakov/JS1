/* 2. Вывести с помощью цикла for числа от 0 до 9, НЕ используя тело цикла. То есть выглядеть должно вот так:
for(…){здесь пусто} */
/**
 * @param  {} count
 */
for (let count = 0; count <= 9; alert(count), count++) {
}